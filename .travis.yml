language: ruby
rvm:
  - 2.5
install: gem install jekyll && gem install s3_website && gem install sass -v 3.4.23 && gem install jekyll-sitemap -v 1.4.0
script: bundle install && bundle exec jekyll build
after_success: s3_website push
